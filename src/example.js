// This file contains additional JavaScript functionality that can be imported into components or used in the application.

export function greet(name) {
    return `Hello, ${name}!`;
}

export function add(a, b) {
    return a + b;
}